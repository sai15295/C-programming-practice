#include <stdio.h>
int main()
{
	int sum = 0,num,i;
	printf("Enter the num:  ");
	scanf("%d",&num);
	for(i=1;i<=num;i++)
	{
		if(i<num)
		printf("%d + ",i);
		else
		printf("%d = ",i);
		sum+=i;
		
	}
printf(" %d",sum);

}